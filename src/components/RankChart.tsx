type Props = {
  className?: string
  items?: { name: string; value: number; icon: string }
}

export const RankChart: React.FC<Props> = (props) => {
  const { className, items } = props
  return (<div>RankChart</div>)
}
